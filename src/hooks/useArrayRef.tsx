// import { useRef } from "react";
// export default function useArrayRef<T>() {
//   const refs = useRef<T[]>([]);

//   function callbackFunc<T> (ref : T) : void{
//     ref && refs.current.push(ref)
//   }
//   refs.current = [];
//   return [refs, callbackFunc];
// }
